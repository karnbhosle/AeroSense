# System Architecture

The system follows a microservice-based architecture:

Frontend (React) → Backend (Node + Express) → ML Engine (Python) → Database (MongoDB)

This architecture ensures modularity, scalability, and independent development of system components.
